<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="csr.security.dao.UpdatePwdDao">

    <select id="ConfirmPassword" resultType="csr.security.entity.SysUser">
        select * from public.t_user where username = #{username} and password = #{password}
    </select>

    <update id="updatePwd" parameterType="csr.security.entity.SysUser">
        update public.t_user set password = #{password} where username = #{username}
    </update>

</mapper>