<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="csr.security.mapper.UserRoleMapper">
    <select id="listByUserId" resultType="csr.security.entity.SysUserRole" parameterType="map">
        select * from public.t_user_role where user_id = #{userId}
    </select>
</mapper>